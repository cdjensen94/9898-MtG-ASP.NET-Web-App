function generateScryfallRegEx() {
const name = ["name"];//-
const set = ["set"];//-
const type = ["type"];//-
const rarity = ["rarity"];//-
const color = ["color"];//-
const mana_cost = ["mana_cost"];//-
const cmc = ["cmc"];//-
const power = ["power"];//-
const toughness = ["toughness"];//-
const loyalty = ["loyalty"];//-
const oracle_text = ["oracle_text"];//-
const flavor_text = ["flavor_text"];//-
const artist = ["artist"];//-
const number = ["number"];//-
const layout = ["]layout"];//-
const multiverse_id = ["multiverse_id"];//-
const image_url = ["image_url"];//-
const price = ["price"];//-
const usd = ["usd"];//-
const eur = ["eur"];//-
const tix = ["tix"];//-
const foil = ["foil"];//-
const nonfoil = ["nonfoil"];//-
const oversized = ["oversized"];//-
const promo = ["promo"];//-
const reprint = ["reprint"];//-
const variation = ["variation"];//-
const set_name = ["set_name"];//-
const set_type = ["set_type"];//-
const set_uri = ["set_uri"];//-
const set_search_uri = ["set_search_uri"];//-
const scryfall_set_uri = ["scryfall_set_uri"];//-
const rulings_uri = ["rulings_uri"];//-
const prints_search_uri = ["prints_search_uri"];//-
const collector_number = ["collector_number"];//-
const digital = ["digital"];//-
const card_back_id = ["card_back_id"];//-
const artist_ids = ["artist_ids"];//-
const illustration_id = ["illustration_id"];//-
const border_color = ["border_color"];//-
const frame = ["frame"];//-
const full_art = ["full_art"];//-
const textless = ["textless"];//-
const booster = ["booster"];//-
const story_spotlight = ["story_spotlight"];//-
const edhrec_rank = ["edhrec_rank"];//-
const penny_rank = ["]penny_rank"];//-
const prices = ["prices"];//-
const related_uris = ["related_uris"];//-
const purchase_uris = ["purchase_uris"];//-
  const name = "name";//+
  const set = "set";//+
  const type = "type_line";//+
  const rarity = "rarity";//+
  const color = "colors";//+
  const mana_cost = "mana_cost";//+
  const cmc = "cmc";//+
  const power = "power";//+
  const toughness = "toughness";//+
  const loyalty = "loyalty";//+
  const oracle_text = "oracle_text";//+
  const flavor_text = "flavor_text";//+
  const artist = "artist";//+
  const number = "card_number";//+
  const layout = "layout";//+
  const multiverse_id = "multiverse_ids";//+
  const image_url = "image_uris";//+
  const price = "prices";//+
  const usd = "usd";//+
  const eur = "eur";//+
  const tix = "tix";//+
  const foil = "foil";//+
  const nonfoil = "nonfoil";//+
  const oversized = "oversized";//+
  const promo = "promo";//+
  const reprint = "reprint";//+
  const variation = "variation";//+
  const set_name = "set_name";//+
  const set_type = "set_type";//+
  const set_uri = "set_uri";//+
  const set_search_uri = "set_search_uri";//+
  const scryfall_set_uri = "scryfall_uri";//+
  const rulings_uri = "rulings_uri";//+
  const prints_search_uri = "prints_search_uri";//+
  const collector_number = "collector_number";//+
  const digital = "digital";//+
  const card_back_id = "card_back_id";//+
  const artist_ids = "artist_ids";//+
  const illustration_id = "illustration_id";//+
  const border_color = "border_color";//+
  const frame = "frame";//+
  const full_art = "full_art";//+
  const textless = "textless";//+
  const booster = "booster";//+
  const story_spotlight = "story_spotlight";//+
  const edhrec_rank = "edhrec_rank";//+
  const penny_rank = "penny_rank";//+
  const prices = "prices";//+
  const related_uris = "related_uris";//+
  const purchase_uris = "purchase_uris";//+
}
>>>>>>> Tabnine >>>>>>>;
